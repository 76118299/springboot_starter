package com.formater.impl;

import com.formater.FormaterProcessor;

public class JSONFormaterProcessor implements FormaterProcessor {
    public <T> String formater(T obj) {
        return "JSON FORMATER";
    }
}
